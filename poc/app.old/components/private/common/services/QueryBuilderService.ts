import * as connectionService from './ConnectionService';

export default {

        composeQuery: (selectedCols: any): string => {

            debugger;
            let select = "";
            let from = "";        
            return "";
        }
    
}

